pandas==1.4.2
scikit-learn==1.1.2
imbalanced-learn==0.9.1 # Instalado con conda install -n mlops_v2 -c conda-forge imbalanced-learn=0.9.1 porque daba probemas sino
hyperopt==0.2.7
mlflow==2.11.3
uvicorn==0.19.0 # Dar accesos al local a la api. Vamoslllll para levantar la api
fastapi==0.110.0 # Para la api

#Librerías de API en Python Django (all bateries included) flask y fastapi

#Django permite hacer paginas web en html con python sin usar otro lengauje pero es muy pesado. Uso específico
#para apis muy grandes (logging de usuarios)

#flask apis ligeras en Python, vas descaragndo poco a poco las librerías que necesites

#FastAPI como flask pero mas rapido, en warmupvideo desde que quieres usarla hasta que esta disponible